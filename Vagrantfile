Vagrant.configure("2") do |config|
  config.vm.box = "builds/almalinux-9.0.parallels.box"
  # temporarily use password authentication as ssh key insertion is not yet working.
  # config.ssh.username = "vagrant"
  # config.ssh.password = "vagrant"
end
